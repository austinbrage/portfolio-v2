/**
 * Locales Index
 * Exports all available language translations
 */

import en from "./en";
import es from "./es";

export var locales = {
  en,
  es,
};

export var availableLanguages = ["en", "es"];